import type { NextConfig } from "next";

const nextConfig: NextConfig = {

  images: {
    domains: ['i.pinimg.com'], // Ajoutez ici tous les domaines que vous utilisez
  },
  /* config options here */
};

export default nextConfig;
